# 产品规格

## 目标
构建一个 CLI 优先的短剧感短篇文本生成系统，把故事想法转成结构化、可迭代优化的短篇故事。

## MVP 边界
- 固定七阶段 harness
- 默认 2 轮 critique-rewrite loop
- 结构化 artifacts 持久化到 `runs/`
- CLI 命令：`run`、`inspect`、`eval`

## 非目标
- 视频生成
- 分镜输出
- Web UI
- 托管式多用户部署
